1) events

2) new element add             
    append()
    prepend()

3) remove 
    remove()
4) text & html
    .text()       => return the text of selected element
    .text(newText) => change the text of selected element
    .html() 
    .html(newHtml) 

5) attributes 
    .attr(attrName)
    .attr(attrName, newValue)

6) css 
    .css('prop')
    .css('prop', 'value')

    .addClass(className)
    .removeClass(className)
    .toggleClass(className)

7) animate 

getter function => get 
setter function => set/change 


8) form values 
    val() 
    val(newValue)

9) dom traversing